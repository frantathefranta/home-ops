---
apiVersion: cilium.io/v2alpha1
kind: CiliumLoadBalancerIPPool
metadata:
  name: l3-pool
spec:
  allowFirstLastIPs: "Yes"
  blocks:
    - cidr: "${cluster_lba_cidr}"
    - cidr: "${cluster_lba_cidr_v6}"
  serviceSelector:
    matchExpressions:
      - { key: not-used, operator: NotIn, values: [not-used] }
